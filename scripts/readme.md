# Note

The subdirectories of this directory contain some scripts intended to act as examples of using FEniCS-SZ as a python package (`fenics_sz`) outside of the jupyter notebooks.  To use these scripts it is necessary to install the `fenics_sz` python package from the `../python` directory:
```
pip install -e .
```